# Airam Music Lights - Python dependencies
#
# UI
PySide6>=6.6.0

# DSP / numerics
numpy>=1.26.0
scipy>=1.11.0

# Windows WASAPI loopback audio capture ("what you hear" recording).
# Plain sounddevice/PyAudio do NOT support WASAPI loopback out of the box;
# PyAudioWPatch is a maintained PortAudio fork that adds it specifically
# for this use case (see DEVICE_NOTES.md).
PyAudioWPatch>=0.2.12.6

# Local (LAN) control of Tuya-based Wi-Fi devices - used to talk to the
# Airam SmartHome bulbs directly without any cloud round-trip.
tinytuya>=1.13.0
